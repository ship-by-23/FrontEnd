# These values are supplied to the production container at runtime.
# The same built image can be promoted from staging to production.
API_URL=https://api.example.com/api/v1
APP_ENV=production
APP_VERSION=commit-sha
